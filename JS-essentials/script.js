const age =23;
const amIAdult = age >= 18 ? true: false;
const names = "Mohan";
const friends= [24, true, "Anup"];

console.log("Name is:",names); //print on browser console
console.log(typeof friends); //check type of variable


//Math operations
const num1 = 10;
const num2 = 20;
const sum = num1 + num2;
const diff = num1 - num2;
const prod = num1 * num2;
const div = num1 / num2;
console.log("Sum is:", sum); //print on browser console

const mod = num1 % num2;
const exp = num1 ** num2; // exponentiation
const neg = -num1; // negation
const abs = Math.abs(num1); // absolute value
const sqrt = Math.sqrt(num1); // square root
const pow = Math.pow(num1, num2); // power
const round = Math.round(num1); // round to nearest integer
const ceil = Math.ceil(num1); // round up   